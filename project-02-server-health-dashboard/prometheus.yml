global:

  scrape_interval: 15s

rule_files:

  - "/etc/prometheus/alerts/server-health-alert-rules.yml"

scrape_configs:

  - job_name: "server-health"

    static_configs:

      - targets:
          - "node-exporter:9100"
