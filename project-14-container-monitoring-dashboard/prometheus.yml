global:
  scrape_interval: 15s

rule_files:
  - "alert-rules.yml"

scrape_configs:

  - job_name: "container-api"

    static_configs:
      - targets:
          - container-monitor-api:8080

  - job_name: "cadvisor"

    static_configs:
      - targets:
          - cadvisor:8080
