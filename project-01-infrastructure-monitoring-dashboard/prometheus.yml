global:
  scrape_interval: 15s

scrape_configs:

  - job_name: "spring-monitor"

    metrics_path: "/actuator/prometheus"

    static_configs:
      - targets:
          - monitoring-api:8080

  - job_name: "node-exporter"

    static_configs:
      - targets:
          - node-exporter:9100
