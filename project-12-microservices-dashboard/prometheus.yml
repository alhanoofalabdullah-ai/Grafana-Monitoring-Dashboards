global:

  scrape_interval: 15s

rule_files:

  - "/etc/prometheus/microservices-alert-rules.yml"

scrape_configs:

  - job_name: "microservices"

    static_configs:

      - targets:
          - "service-a:8080"
          - "service-b:8080"
          - "service-c:8080"
