input {
  file {
    path => "/logs/application.log"
    start_position => "beginning"
  }
}

filter {

}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "application-logs"
  }
}
