global:
  scrape_interval: 15s

rule_files:
  - "alert-rules.yml"

scrape_configs:
  - job_name: "api-monitor-service"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["api-monitor-service:8080"]
