global:
  scrape_interval: 15s

rule_files:
  - "alert-rules.yml"

scrape_configs:

  - job_name: "linux-api"

    static_configs:
      - targets:
          - monitoring-api:8080

  - job_name: "node-exporter"

    static_configs:
      - targets:
          - node-exporter:9100
