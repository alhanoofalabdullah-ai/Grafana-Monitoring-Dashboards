global:

  scrape_interval: 15s

rule_files:

  - "/etc/prometheus/application-alert-rules.yml"

scrape_configs:

  - job_name: "application"

    static_configs:

      - targets:
          - "application:8080"
