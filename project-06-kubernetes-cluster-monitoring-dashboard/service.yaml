apiVersion: v1
kind: Service

metadata:
  name: cluster-monitor-service

spec:
  selector:
    app: cluster-monitor

  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080

  type: LoadBalancer
