groups:

- name: security-alerts

  rules:

  - alert: CriticalSecurityAlert

    expr: security_critical_alerts > 10

    for: 1m

    labels:
      severity: critical

  - alert: BruteForceAttackDetected

    expr: failed_login_attempts > 100

    for: 2m

    labels:
      severity: warning
